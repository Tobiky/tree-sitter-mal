=======================================
Attack-step with identity Pre-condition
=======================================

category Test {
    asset TestAsset {
        | onePrecon
            <- a
        | multiplePrecon
            <- a, b
    }
}

---------------------------------------

(source_file
    (category_declaration
        id: (identity)
        assets: ((asset
                    id: (identity)
                    steps: ((attack_step
                                id: (identity)
                                preconditions: ((asset_expr (identity))))
                            (attack_step
                                id: (identity)
                                preconditions: ((asset_expr (identity))
                                                (asset_expr (identity)))))))))
